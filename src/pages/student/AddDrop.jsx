export default function AddDrop() {
  return (
    <div>
      <h1>Add / Drop Courses</h1>

      <p>You can add or remove courses here.</p>

      <button>Add Course</button>
      <button>Drop Course</button>
    </div>
  );
}
